#include <stdio.h>

int main() {
    int numero = 0;
    printf("Entre com um numero inteiro positivo.");
        scanf("%i", &numero);
        while(getchar() != '\n');
    do {
        fat = n * (n - 1)
       
    } while()

    return 0;
}